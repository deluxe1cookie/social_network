import React from "react";
import styles from './Feed.module.css';

const Feed = (props) => {
    return (
        <div className={styles.messages}>
            Feed
        </div>
    )
};

export default Feed;